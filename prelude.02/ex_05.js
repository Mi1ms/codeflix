module.exports = function flatten(arr, shallow) {
    let arr_clean = [];
    for (let val of arr) {
        console.log(typeof val);
        if (typeof val == "object") {
            
        }
    }    
}

